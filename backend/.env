require('dotenv').config({ path: './backend/.env' });

# SQL Server Configuration (SQL Server Authentication)
DB_SERVER=localhost
DB_DATABASE=MyAppDB
DB_USER=sa
DB_PASSWORD=12345678
DB_TRUST_CERTIFICATE=true
DB_ENCRYPT=false

# SQL Server Port (optional, default is 1433)
DB_PORT=1433

# Backend Server
PORT=5000
NODE_ENV=development